#include "Renderer.hpp"

namespace Graphics {

    Renderer::Renderer()
    {
    }

    void Renderer::Render(f32 dt)
    {
        (void)dt;
    }

    void Renderer::Resize(u32 width, u32 height)
    {
        (void)width;
        (void)height;
    }

}
